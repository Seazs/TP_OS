#include <pthread.h>
#include <semaphore.h>

void* fonction_qui_compte_beaucoup(void* args) { return NULL; }

int main(int argc, char const* argv[]) {
  //   pthread_mutex_t mutex;
  //   pthread_mutex_init(&mutex, NULL);

  // A compléter
  return 0;
}
